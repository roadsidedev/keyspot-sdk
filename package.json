{
  "name": "agentguard-sdk",
  "version": "2.0.0",
  "description": "Runtime security layer for autonomous AI agents",
  "type": "module",
  "scripts": {
    "test": "node --loader ts-node/esm test-v2.ts",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "@types/node": "^20.0.0"
  }
}
