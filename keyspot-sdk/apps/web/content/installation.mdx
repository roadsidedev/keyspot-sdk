---
title: Installation
description: How to install KeySpot SDK
---

# Installation

KeySpot SDK is published as a single package under the `@roadsidelab` scope. One install, everything included.

```bash npm2yarn
npm install @roadsidelab/keyspot-sdk
```

## Package Overview

| Import path | Description | Heavy deps? |
| --- | --- | --- |
| `@roadsidelab/keyspot-sdk` | KeySpot, Scanner, TaintEngine, PromptShield, Vault, Audit | No |
| `@roadsidelab/keyspot-sdk/adapters` | Vector store adapters (Chroma, Pinecone, Qdrant, etc.) | Yes* |
| `@roadsidelab/keyspot-sdk/frameworks` | Framework wrappers (LangChain, Anthropic, OpenAI) | No |
| `@roadsidelab/keyspot-sdk/cli` | CLI commands (`keyspot scan`) | No |

*\* Heavy deps are optional — only downloaded if you use `/adapters`.*

## Requirements

- **Node.js** ≥ 18
- **TypeScript** ≥ 5.0 (recommended)
